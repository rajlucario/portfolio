import React from 'react'
import ReactDOM from 'react-dom/client'
import RajPortfolio from './rajportfolio.jsx' // Note the new file name and path
import './index.css' // Your default CSS file

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <RajPortfolio />
  </React.StrictMode>,
)
